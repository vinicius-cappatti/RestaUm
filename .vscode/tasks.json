{
    "tasks": [
        {
            "label": "C/C++: gcc arquivo de build ativo",
            "type": "shell",
            "command": "/usr/bin/gcc", // ou o caminho correto do GCC no seu sistema
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Tarefa gerada pelo plug-in C/C++."
        }
    ],
    "version": "2.0.0"
}